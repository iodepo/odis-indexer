services:
  floci:
    image: floci/floci:latest          # or :latest-jvm
    container_name: odis-floci-s3
    ports:
      - "4566:4566"
    volumes:
      - ./floci-data:/app/data        # Persist data locally
    restart: unless-stopped